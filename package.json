{
  "name": "test-sdk-test",
  "version": "1.0.1",
  "description": "first test sdk for me",
  "main": "dist/main.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist & cross-env NODE_ENV=production webpack --config webpack.config.js"
  },
  "keywords": [
    "test"
  ],
  "author": "ly",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "path": "^0.12.7",
    "rimraf": "^3.0.2"
  }
}
